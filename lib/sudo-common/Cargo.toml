[package]
name = "sudo-common"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0 OR MIT"

[dependencies]
pam-client = "0.5"
sudo-system = { path = "../sudo-system" }
which = "4.4.0"

[dev-dependencies]
sudo-cli = { path = "../sudo-cli" }
